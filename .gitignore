# Cloned upstream checkout (has its own .git; recreate with:
#   git clone https://github.com/exasol-labs/language-container-rs repo
#   git -C repo apply ../mojo-bridge.patch
/repo/

# Build outputs
native-mojo/out/
native-mojo/mojoudfclient
native-mojo/**/*.tar.gz

# Python / OS cruft
__pycache__/
*.pyc
.DS_Store
